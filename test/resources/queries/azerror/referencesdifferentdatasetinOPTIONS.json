{
	"title": "Invalid Query - InsightError - referenced a dataset with the id: 'id' instead of 'courses' in OPTIONS",
	"input": {
		"WHERE": {
			"IS": {
				"courses_dept": "cpsc"
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_dept",
				"courses_avg",
				"courses_uuid"
			],
			"ORDER": "id_avg"
		}
	},
	"errorExpected": true,
	"expected": "InsightError"
}
