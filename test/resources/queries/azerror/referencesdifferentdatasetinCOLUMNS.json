{
	"title": "Invalid Query - InsightError - referenced a dataset with the id: 'id' instead of 'courses' in COLUMNS",
	"input": {
		"WHERE": {
			"IS": {
				"courses_dept": "cpsc"
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"id_dept",
				"courses_avg",
				"courses_uuid"
			],
			"ORDER": "courses_avg"
		}
	},
	"errorExpected": true,
	"expected": "InsightError"
}
